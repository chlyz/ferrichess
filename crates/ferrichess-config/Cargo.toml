[package]
name = "ferrichess-config"
version = "0.1.0"
edition = "2024"
description = "Shared user configuration for Ferrichess tools"
license = "GPL-3.0-or-later"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.9"
